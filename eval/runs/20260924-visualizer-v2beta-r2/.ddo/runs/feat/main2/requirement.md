# 用户需求

> 用户触发此流水线的原始需求描述。

## 原始需求

开发个本地 ddo-code-flow 工作流可视化工具：语言用 node，通过从用户根目录的索引文件检查到本机执行过程中的 .state.json 来可视化执行流程，html 渲染的图都用 svg 来画，线不要出现相交的情况。

## 需求摘要

开发一个本地 Node 工作流可视化工具，从用户根目录索引文件与本机执行过程的 .state.json 读取执行流程，以 SVG 在 HTML 中渲染，且连线不得相交。

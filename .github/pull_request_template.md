## 变更说明

<!-- 做了什么、为什么；一两句话即可，细节让 diff 和台账自己说话 -->

## 关联

<!-- 关联的 issue（#N）或工作项（.ddo/runs/feat/<dir>/）；都没有就写「无」 -->

## 流程符合性

- [ ] 涉及流程语义 / 命令行为 / schema 的改动：已走 ddo 流程（requirement → plan 门评审 → 实现），台账在 `.ddo/runs/`
- [ ] 不涉及上述内容的小改动（文档错字、测试补充等）：可豁免，勾选此项说明原因

## 测试

- [ ] `node --test tools/tests/*.test.js` 全绿（用例数：`<!-- 填写，如 78/78 -->`）

## 文档同步

命令行为 / 目录语义 / 预设变化时须同步（无变化勾选说明）：

- [ ] `tools/cli.js` 命令注册表（命令自描述）
- [ ] `SKILL.md`（agent 侧协议）
- [ ] `README.md`（人读侧）

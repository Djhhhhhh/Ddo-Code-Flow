# Context Summary

## 已加载来源

- 无。项目根目录不存在 `AGENTS.md`，且有效配置 `base.contextPaths` 为空数组，本次 run 没有声明任何项目级基线上下文路径。

## 上下文缺失

- 无。未声明任何必读的上下文路径，因此不存在缺失项。

> 说明：本项目为 ddo-code-flow skill 的自托管仓库。项目根 `CLAUDE.md` 仅指向 `.claude/rules/v4-responsibility-boundaries.md`（四层职责边界规则）。下游 spec/plan 阶段会在 worktree 中直接读取 skill 源文件（SKILL.md、schemas、workflows、atom-tasks）以建立项目事实。

# task-10: SKILL.md 改写（Step 1-7 委托子命令）

- 关联验收点：G9（AC-9，FR-SKILLMD-8）
- 依赖：task-01 … task-09
- 状态：pending

## 目标

把 SKILL.md 的散文 Step 1–7 改写为委托 `node ddo.js <子命令>`，模型只做生成、不做解释器。放在最后，等代码 + 测试全绿再改，可单独回滚。

## 涉及文件

- `SKILL.md`（修改）

## 实现要点

- 每个 Step 保留「何时调、为什么调、非零退出码怎么进修正循环」的散文纪律（触发仍是软约束），但「怎么做」交给子命令。
- 删除大段散文状态机（DAG 校验 / 角色注入 / 状态归属 / 门控的实现细节），净上下文下降。
- 不改变四层职责模型；不写 skillRoot；不改 `.gitignore`。

## 验收

- [ ] human: 检查 Step 1–7 已改为委托子命令，无遗留大段散文状态机；四层职责模型未被破坏。

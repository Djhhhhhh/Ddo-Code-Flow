{
  "name": "coding",
  "version": "2.0.0",
  "desc": "按任务清单/plan 实施步骤执行真实代码改动（产物是代码变更本身）",
  "defaults": {
    "extra_ctx": "",
    "rules": [
      "所有代码改动必须落在 git.worktreePath 指向的工作树内",
      "不得为通过检查而注释/删除既有测试，除非任务本身要求"
    ]
  }
}
